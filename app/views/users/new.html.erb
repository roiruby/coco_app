<div class="container">
  <div class="row">
    
    <div class="offset-md-3 col-md-6 mt-4">
      <p class="bold h5 mb-1 ml-1 text-center">Sharingood 新規会員登録</p>
      <p class="text-center small">Sharingoodに会員登録いただくと、プランナーになり</br>
        投稿やプランナーのフォローを行う事ができます。</br>
        登録はたったの2ステップ、1分でOK!!</p>

      <div class="bg-gray mb-5">
      <%= form_with(model: @user, local: true) do |f| %>
        <%= render 'layouts/error_messages', model: f.object %>

        <div class="form-group">
          <%= f.label :name, 'ユーザーネーム' %>
          <%= f.text_field :name, class: 'form-control' %>
        </div>

        <div class="form-group">
          <%= f.label :email, 'メールアドレス' %>
          <%= f.email_field :email, class: 'form-control' %>
        </div>

        <div class="form-group">
          <%= f.label :password, 'パスワード' %><span class="xsmall">　※[半角英数字]6文字以上</span>
          <%= f.password_field :password, class: 'form-control' %>
        </div>
        
        
        <div class="text-center mt-3">
          <%= f.submit '登録する', class: 'btn btn-info bold' %>
        
      <% end %>
      

      
      </div>
      </div>
    </div>
    
  </div>
</div>